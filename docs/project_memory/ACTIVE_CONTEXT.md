# Active Context

Last reviewed: 2026-08-10.

## Current Goal

Stabilize the six-step operator flow around Windows `0.1.9`. The installed contact-enrichment path has a real logged-in positive and negative sample check, strict PGY-to-XHS creator binding, stable-profile readiness, and trustworthy creator names. Installed-app candidate acceptance now covers both global first-50 mutation and a direct rank 35-50 continuation through the rank-40 checkpoint. Clean-machine installation, signing/SmartScreen, and the formal sanitized PGY live-safety record still require separate acceptance. Preserve the 50-creator run ceiling, manual rank-40 checkpoint, fail-closed pagination, and human approval gate for all actual sending.

## Completed Important Stages

- On 2026-08-10, Windows `0.1.9` fixed three live contact-review defects: saved XHS URLs can no longer leak across creators, profile identity is derived from the verified PGY creator-detail identity instead of page-wide links or a brittle click, and an unrelated nested loading indicator no longer masks stable visible profile content. Candidate-list names now remain the review identity unless the exact PGY creator-name region provides a verified replacement; broad company/account selectors were removed.
- A real installed-app, logged-in, low-frequency test reused three explicitly selected rows and persisted sanitized outcomes: one public email found, two `CONTACT_NOT_PUBLIC`, and zero technical failures. All three stored profile route identities matched their PGY creator identities. The full desktop test chain passed twice after the final resolver change. After commit `b65d1b5`, the Windows x64 installer was rebuilt from the committed source: `desktop-app/dist/小红书相关信息采集工具-0.1.9-windows-x64-setup.exe` is 108,690,383 bytes with SHA-256 `3986048A36BFD76242759A7BF57115241A759672F152A3DD4D83451DB3C23264`. The `win-unpacked` executable and a silent same-version NSIS installation both started their own packaged backend with protocol 1 and a non-empty instance token; closing the unpacked app released port 8010 and stopped its backend. The installed product reports `0.1.9`, was reopened without remote debugging, and the installer remains unsigned. Install, process, service, login, artifact, and remote facts are volatile and must be rechecked.
- The installed `0.1.9` candidate workflow completed two separate logged-in cross-page acceptance runs. `前50位` read `20 + 20 + 10`; direct `35-50位` read `6 + 10`. Both stopped after global rank 40, enforced the 90-second manual cooldown, continued only after operator confirmation, restored the protected result page to page 1, produced a unique latest segment fully contained in the deduplicated candidate pool, and showed no risk signal. These runs only added candidates; they did not start a 50-person creator-detail collection. This evidence does not replace the formal sanitized small-batch, exact-10, and risk-stop safety record.
- On 2026-08-06, Windows `0.1.7` separated control requests from effective states for both creator collection and contact enrichment. The UI now distinguishes collecting, pending pause, paused, pending stop, stopped, and completed states; whole-run stop takes effect at the next safe point and marks the unfinished queue as skipped rather than continuing in the background. The task and contact views can return to the protected PGY result page, preferring the matching BrowserView history entry and warning when only a fresh fallback URL is available.
- Contact enrichment now polls the PGY creator page for a direct XHS profile link or stable link evidence, uses interruptible page/cooldown waits, and persists a categorized outcome code plus readable failure reason per creator. Saved XHS profile URLs bypass future PGY resolution. Calibration now shows the current step and next verification action, and routes current-page and cross-page checks back to the result tab first.
- The complete desktop test chain, renderer/main syntax checks, `scripts/audit_pgy_safety.py`, and project-memory checks passed. `scripts/check_mvp_readiness.py` reported `pass=32 fail=0 block=1 warn=0`; the sole block remains the missing sanitized real-account validation record. Windows x64 `0.1.7` artifacts were built: installer `desktop-app/dist/小红书相关信息采集工具-0.1.7-windows-x64-setup.exe` (108,689,487 bytes, SHA-256 `6E779B284040D5615B57E39AC62649160B9BD3B1342A92B1FFC32C7DF4831AF7`) and portable `desktop-app/dist/小红书相关信息采集工具-0.1.7-windows-x64-portable.exe` (94,162,785 bytes, SHA-256 `28532DF076DC4E63DFE36134830D35E2C13421B0D78D0453C256D151685FD297`). The local installed product reports `0.1.7.0`; packaged directory, portable, and installed launches returned the expected protocol/token-owned backend health and cleaned up in the close smoke tests. The installed app was relaunched and left open with `服务正常 / v0.1.7`; a read-only screenshot showed the start view without overlap. Artifact, installed, service, port, auth, and remote facts are volatile and must be rechecked.
- On 2026-08-06, live logged-in calibration separated the creator row (`div.kol-info_detail`), nickname field (`.kol-name`), and semantic pagination root (`.d-pagination.hide-pagination-page-size`). The app first rejected the stale row/name-equal template, then read 20 pure nicknames on page 1 and completed a read-only `1 -> 2 -> 1` validation with 20 creators on both pages. Picker results now distinguish the exact clicked path, the repeated field selector, and the repeated container selector; row/name equality is rejected in both renderer validation and main-process pagination gates. No candidate was added and no first-50 mutation was run.
- The 31-file desktop test chain, `scripts/audit_pgy_safety.py`, `scripts/verify_project_memory.py`, renderer/main syntax checks, and `git diff --check` passed. Windows x64 `0.1.6` artifacts were built: installer `desktop-app/dist/小红书相关信息采集工具-0.1.6-windows-x64-setup.exe` (108,702,901 bytes, SHA-256 `D8ACFC7F7815697F6CAA7A702DEF86852C8E3D46E2CFA252808FB38FF8177A45`) and portable `desktop-app/dist/小红书相关信息采集工具-0.1.6-windows-x64-portable.exe` (94,158,223 bytes, SHA-256 `445F8CBC2974E3C89CC09911C14537AB90FE3FA483E4104FEFD22A8391460ED4`). Local upgrade from `0.1.5` returned exit code 0; the installed UI showed `服务正常 / v0.1.6`, packaged health protocol/token/PID ownership matched port 8010, a second launch kept one renderer and the same backend PID, the installed calibration view loaded, and closing the app released the backend and ports. The portable artifact also started from its self-extracted directory with `服务正常 / v0.1.6`, owned its packaged backend, and released the backend and ports on close. Artifact, installed, branch, service, login, and remote facts are volatile and must be rechecked after rebuild or in a new thread.
- On 2026-08-05, a visible logged-in PGY result page showed that page 1 contains roughly 20 creators, confirming that `first 50` is a global-rank request that must continue through pages 2 and 3. The prior reader already had a bounded page loop, but could fail before the first turn when the visible list had rendered without a usable page-tagged response. `0.1.5` can bind a sanitized same-context response to the visible page by ordered creator names, and can read current-page rows from Vue or React component data as a read-only fallback. Every accepted page still requires visible page identity, a changed fingerprint, no adjacent-page overlap, and no PGY risk signal; incomplete results are not merged.
- The 30-file desktop test chain and `scripts/audit_pgy_safety.py` passed. Windows x64 artifacts were built: installer `desktop-app/dist/小红书相关信息采集工具-0.1.5-windows-x64-setup.exe` (108,701,987 bytes, SHA-256 `09301148C80155887A9542584C5DE84E971169AFBB5DC4745ED5FCD2B7E7639E`) and portable `desktop-app/dist/小红书相关信息采集工具-0.1.5-windows-x64-portable.exe` (108,474,124 bytes, SHA-256 `DAA1BFEFEC2A1DF6EEEBD8527613513E52504751894F787C453092F6320C6188`). Local installation returned exit code 0; the installed product reports `0.1.5.0`, the UI shows `服务正常 / v0.1.5`, `/api/desktop/health` returned protocol 1 and the packaged backend PID, and installed ASAR inspection confirmed visible-name binding, component fallback, and React row discovery. The app reopened at `about:blank`, so logged-in cross-page acceptance is still pending and must not be inferred from these checks.

- On 2026-08-05, user evidence showed the visible PGY paginator unambiguously on page 1 while the app still reported that it could not return to page 1. The reader had two defects: it discarded disabled pagination controls even though the disabled previous arrow is first-page evidence, and it preferred recursively discovered response metadata over the visible paginator. `0.1.4` keeps disabled controls for evidence, recognizes highlighted page 1 or a disabled previous control, makes visible pagination primary, and limits payload page metadata to the candidate-list branch. Conflicts remain fail closed and do not merge partial candidates.
- Creator-detail and XHS navigation no longer reuse the protected collection BrowserView. A dedicated `xhs-profile` role tab now handles PGY creator detail resolution, manual XHS login, profile reads, and contact enrichment, then returns to the preserved collection tab after a batch. The contact-review renderer separately restores list position by run/filter key and creator anchor across progress-driven rerenders, with the active creator visibly marked.
- XHS contact enrichment replaced the fixed 5.5-9 second sleep plus single read with bounded polling of canonical target URL, document/profile evidence, and consecutive stable visible snapshots. Login and risk evidence always wins. Public parsing now includes visible `mailto:` links and full-width/spaced or bracket-obfuscated email punctuation without persisting full profile text.
- The 30-file desktop test chain, `scripts/audit_pgy_safety.py`, and `git diff --check` passed. `scripts/check_mvp_readiness.py` remained `pass=32 fail=0 block=1 warn=0`; the sole block is the missing sanitized real-account validation record. Windows x64 artifacts were built: installer `desktop-app/dist/小红书相关信息采集工具-0.1.4-windows-x64-setup.exe` (108,699,796 bytes, SHA-256 `3D26E8F0149DC9DCB0360E366C006EA6F6E3707854A95E133765F75FE3C9A5EB`) and portable `desktop-app/dist/小红书相关信息采集工具-0.1.4-windows-x64-portable.exe` (108,471,927 bytes, SHA-256 `AA2A931365071507F94F1B58ABC17D8792934CC54FEF25163E144342AC34A840`). Local upgrade returned exit code 0; installed product version is `0.1.4.0`, the visible UI showed `服务正常 / v0.1.4`, installed ASAR inspection confirmed all four fixes, and closing the app stopped the packaged backend and released port 8010. No real PGY/XHS page was opened during installed acceptance.

- On 2026-08-04, two installed `0.1.2` screenshots showed both first-15 and first-50 candidate instructions failing at rank 0 while the visible PGY list already contained creators. Root cause: the debugger discarded list responses received before the instruction command window opened, and the current PGY rows did not expose detail links for the DOM fallback. `0.1.3` continuously reduces candidate-shaped responses to sanitized rows, binds each snapshot to the collection BrowserView plus main-navigation epoch, and seeds a command only from that same navigation context. Cross-command, cross-navigation, page-conflict, duplicate-page, and overlap checks remain fail closed. A missing snapshot now returns `PGY_CANDIDATE_RESPONSE_NOT_READY` instead of misleadingly reporting rank 0.
- All 29 desktop tests and `scripts/audit_pgy_safety.py` passed after adding the page-loaded-before-command regression. Windows x64 artifacts were built: installer `desktop-app/dist/小红书相关信息采集工具-0.1.3-windows-x64-setup.exe` (108,700,285 bytes, SHA-256 `CB0887B1348DA211C02BFC91149A563BE83F31211B632D5888D61D2CC1D6F45C`) and portable `desktop-app/dist/小红书相关信息采集工具-0.1.3-windows-x64-portable.exe` (94,137,583 bytes, SHA-256 `677E6A602BDE95225DE4D857C38580788FF810184FA658937042C6E9921778F7`). Local upgrade succeeded; installed product version is `0.1.3.0`, visible UI showed `服务正常 / v0.1.3`, and installed ASAR inspection confirmed the snapshot seed, passive sanitized capture, and clear error path. Closing the app stopped the backend. PGY required account login after restart, so no real candidate read was performed during installation QA.
- On 2026-08-03, Windows `0.1.2` closed the main reliability gaps found in the July screenshots and code review. Candidate response capture is scoped to the active instruction and rejects cross-page overlap; repeated search merges preserve operator-owned candidate status/priority/exclusion reason/note; drafts autosave locally; task pause/skip states are explicit and unfinished runs can recover from schema-v2 `task_state.json`; export run switching waits for run-scoped autosave; email handoff no longer silently starts XHS enrichment; renderer failures are isolated; automation jobs share one operation lock; packaged startup verifies a per-launch backend token/protocol and Electron is single-instance. All 29 desktop test files, PGY safety audit, project-memory validation, live-validation unit tests, and MVP checks passed with `pass=32 fail=0 block=1 warn=0`. The only readiness block remains the missing sanitized real-account validation record.
- Windows x64 artifacts were built and hashed: installer `desktop-app/dist/小红书相关信息采集工具-0.1.2-windows-x64-setup.exe` (108,702,651 bytes, SHA-256 `84FEFDB15CBD6DCEA322E98A0CF2B0983E91BE400BF9299163ED7FFD346C49C1`) and self-contained portable `desktop-app/dist/小红书相关信息采集工具-0.1.2-windows-x64-portable.exe` (94,135,406 bytes, SHA-256 `A130ECEAE8ECE12EBF2A65868B1D551C05376842D6FEB63C1FE8BD178F4FC4B8`). The portable and installed builds visibly showed `服务正常`, `v0.1.2`, the protected `采集页` tab, and a separate closable `企业邮箱` tab. A second launch kept one window and the same backend PID; close released port 8010. The local `0.1.1` installation was upgraded to product version `0.1.2.0`, and the desktop shortcut contains the current user's install path. Recheck all artifact facts after any rebuild.
- On 2026-07-31, user screenshots exposed two separate delivery defects. The daily-use Windows installation was still the July 27 `0.1.0` ASAR with no tab implementation, and its desktop/start-menu shortcuts targeted a nonexistent sandbox-user path. Version `0.1.1` now displays its version and a stronger `网页标签 / 采集页 / + 企业邮箱` strip. Candidate pagination reads page metadata from the in-memory list response, compares ordered creator fingerprints, treats the numeric active-page DOM marker as supporting evidence, and can use the visible jump input to return to page 1. Explicit response-page conflicts, duplicate pages, missing responses, and risk prompts still fail without merging partial results. All 28 desktop tests, the PGY safety audit, project-memory verification, syntax checks, and `git diff --check` passed. The rebuilt `win-unpacked` app visibly showed `v0.1.1`, opened enterprise mail in a separate closable tab, preserved the collection tab, reached `服务正常`, and released port `8010` on close. Replacement installation, shortcut repair, and live PGY range testing remain to be checked.
- On 2026-07-27, the Windows x64 packaging chain was implemented and verified on Windows 11 x64. `desktop-app/scripts/build-backend.js` built a PyInstaller `--onedir` backend, Electron now resolves `xhs-pgy-backend.exe` on Windows and fails closed when a packaged backend is missing, and `package.json` gained `dist:win:dir` and `dist:win`. Recording now excludes password, account, phone, email, and verification-code inputs; Windows path guards handle case-insensitive paths; backend cleanup also runs on `before-quit`. All 25 desktop test files, project-memory verification, the PGY safety audit, MVP-readiness tests, and live-validation tests passed. The packaged backend started from `win-unpacked/resources/content-analyzer-backend`, `/api/config` responded, proxy/headless were off, the legacy crawl route returned 403, and no backend process remained after shutdown. The final ASAR contains the privacy module and the NSIS installer is `desktop-app/dist/小红书相关信息采集工具-0.1.0-windows-x64-setup.exe` (108738505 bytes, SHA-256 `6B6E5B5A31DF30CD3B8F809442E15F4BFAF7408A5F07327FFA18BC11EA40B475`). It is unsigned and has not yet passed an actual NSIS install/upgrade/uninstall, clean-machine, visual BrowserView, workbook export, or real-account acceptance; all of those need recheck.
- On 2026-07-24, GitHub device authorization was completed and `main` plus 10 validated snapshot tags were pushed to `https://github.com/toastphoto/xhs-pgy-data-collector`. The remote `main` included `c726ae4` and the preceding 19 locally ahead commits at the time of verification. Real `.env` files, DMGs, build outputs, and local session data remained ignored and were not pushed. Authentication, network, and remote state remain volatile and must be rechecked.
- On 2026-07-24, the repository state was rechecked as `main` at `c37a8e3`, clean and 18 commits ahead of `origin/main`; remote sync, running services, and authenticated sessions remain volatile. The Apple Silicon DMG still matches SHA-256 `278fb1de97d8234f81b004133fb49d60d725fa545e01a1e3c09f046833266eea`. It is not a Windows artifact; Windows packaging and live validation remain pending.
- On 2026-07-23, the current visible-candidate-reset build was packaged as an Apple Silicon DMG at `releases/2026-07-23/小红书相关信息采集工具-2026-07-23-醒目清空候选版-arm64.dmg` (194042627 bytes, SHA-256 `278fb1de97d8234f81b004133fb49d60d725fa545e01a1e3c09f046833266eea`). macOS DMG verification, all 23 desktop tests, the PGY safety audit, and project-memory verification passed. The bundle keeps only the placeholder `.env.example` and excludes real `.env` and common private variants; AI credentials remain user-entered local app settings. The package is not Apple Developer signed or notarized, so Gatekeeper behavior on another Mac must be rechecked.
- On 2026-07-23, the candidate panel gained an always-visible red `清空全部候选` action beside its search/status controls. It clears the full candidate pool, latest segment, filter/query state, and import/search preview only after an explicit confirmation; completed runs, collected results, and exported files are not deleted. The older filtered deletion remains inside batch tools with the clearer label `移除当前显示`.
- The installed app was checked at the real workbench width with one synthetic PGY candidate: the new action stayed visible without overlap, its confirmation described the exact scope, and confirming returned the candidate count to zero and removed the synthetic test URL. All 23 desktop test files, syntax checks, and the PGY safety audit passed. No live PGY read, collection, enrichment, export, or outreach was triggered.
- Source checkpoint: `7f4e866` / `snapshot-2026-07-23-visible-candidate-reset`. The local ignored recovery archive is `releases/2026-07-23/小红书相关信息采集工具-2026-07-23-醒目清空候选版.app.zip`, SHA-256 `f85f62579f22d02ce3ccc0c7b1491176ff9eaeff2d4c516d3ec3db283f37fe0a`; ZIP integrity passed. Remote sync and current app/session state remain volatile.
- On 2026-07-23, candidate commands gained explicit rank ranges such as `第42位到第50位` and `第50位到第70位`. Rank position and batch size are validated separately: the farthest supported rank is 100, while one intake/collection segment remains capped at 50. Multi-page reads start from rank 1, stay bounded to 10 response pages, recheck PGY risk before each page turn, wait conservatively, deduplicate, and restore the original page only when no risk signal is active.
- The task page now persists a `最近加入的一段` scope. A successful range instruction merges creators into the durable candidate pool but switches the next collection run to only that latest segment, so later batches do not repeat earlier candidates or exceed the 50-person run ceiling. Saved signing tasks and candidate-sheet exports preserve this scope.
- All 23 desktop test files, syntax checks, the extended PGY safety audit, and project-memory verification passed. The packaged app was installed to `/Applications`, its backend reached `服务正常`, and the live UI showed the new range prompt and latest-segment scope. No PGY result read, page turn, collection, enrichment, export, or outreach was started during installation QA. Live range ordering, pagination, restore, and risk-stop behavior remain pending.
- On 2026-07-22, the contact-review cards gained a restricted Pugongying profile link that opens only an allowlisted `pgy.xiaohongshu.com` creator URL in the visible BrowserView. The prominent per-card execution-channel chip was removed; underlying channel/export data remains intact.
- Added a Tencent enterprise-mail handoff for selected creators in the current review filter who have valid emails. It opens the official visible mailbox page, waits for manual login, may click only a semantic compose action, and fills only the recipient field. It never fills credentials, invokes a mail API, fills content, or clicks send. The action is capped at 20 unique valid recipients, requires an in-app confirmation, stops on security/risk text, and warns about multi-recipient privacy.
- The 23-test desktop chain, syntax checks, PGY safety audit, and project-memory verification passed on 2026-07-22. An isolated dummy-data UI test verified recipient-count changes, the confirmation/cancel gate, visible Pugongying links, and the official Tencent enterprise-mail landing page. Post-login recipient filling still needs a user-authorized live mailbox test; no real email or invitation was sent.
- On 2026-07-21, reworked the task page around a natural-language candidate instruction while keeping Excel and pasted-link import. The start action can hand a completed run to visible, serial public-XHS-contact enrichment; outreach copy and export remain in the review page.
- The live PGY creator plaza test successfully added the current first 3 creators in order. A request for 51 creators was rejected by the 50-person hard limit. No cooperation, invitation, email, WeChat, or other send action was triggered.
- Because the current PGY list does not expose detail links, candidate intake now has an in-memory read-only response parser. It retains only normalized public creator IDs, names, and metrics; it does not persist response bodies, headers, cookies, or tokens.
- Low-frequency pagination for up to 3 result pages/50 creators is implemented with pre-page risk checks, waits, deduplication, and best-effort return to the starting page. Live first-30/50 validation remains pending.
- The 22-test desktop chain and `scripts/audit_pgy_safety.py` passed on 2026-07-21. This iteration was saved as a local Git checkpoint; immediately afterward `main` was two commits ahead of `origin/main` with a clean worktree. Remote state remains volatile.
- On 2026-07-21, fixed the primary contact-workbook export feedback. Export had succeeded, but its only status message was rendered after the long secondary-export section and looked unresponsive. The primary action now shows a working state, nearby success/error feedback and path, and an immediately enabled open-file action. A live 20-row export was reread successfully with all 6 expected sheets and 16 email rows; no outreach was sent.
- `/Applications/小红书相关信息采集工具.app` now contains the latest source changes, the matched standalone backend resource, pagination code, and export feedback fix. The first-30/50 PGY pagination behavior still needs its separate live validation.

- On 2026-07-20, fixed contact-review row identity collisions caused by truncating the Base64 encoding of long Pugongying URLs. Row IDs now hash the complete stable source identifier; legacy contact enrichment migrates only when the XHS profile entity ID matches the Pugongying creator entity ID. Regression tests cover shared URL prefixes and one-creator-only email migration.
- Added a current-filter batch action for changing contact channel. The existing 50-person run was restored to 50 Pugongying-invite rows and re-exported; verification found exactly one email row, belonging to “是谢谢啦”. No invite was sent.
- Email contact export is now a contact-capability view instead of an exclusive primary-channel route: every selected creator with an email is included in `邮件建联表`, while their Pugongying invite row remains intact. The 50-person run now exports 50 Pugongying rows and one email row for “是谢谢啦”; no message was sent.
- On 2026-07-20, a visible low-frequency live test processed the first 20 creators in the saved 50-person order: 20 completed, 12 public-contact hits, 8 with no public contact, and 0 failures. All 12 hits were emails; no reliable WeChat ID or phone was found, and no login/captcha/risk pause occurred.
- Added a reusable first-N enrichment control, defaulting to 20 with a 50-person ceiling. The verified workbook contains 50 contact rows, 50 Pugongying rows, 13 email rows including the previously enriched creator, and 0 XiaoMiFeng rows. No outreach or execution approval was triggered.

- Phase 1 source implementation added on 2026-07-15: backend status is queried on renderer startup and refreshed every 10 seconds instead of relying on one startup event.
- Added a dedicated XiaoMiFeng workbook export with the exact five business columns from the provided template, its help header, and blank `Sheet2`/`Sheet3`. Only selected creators with a WeChat ID or phone number are eligible.
- Added a XiaoMiFeng approval fingerprint bound to channel, run, selected recipients, message, smart-remark rule, tag, and executor WeChat. A changed payload invalidates approval, and no execution workbook can be generated without a valid current approval.
- Added Feishu contracts for tasks, task creators, execution batches, approvals, send events, and reply events. Actual Feishu writes remain pending until Base/table/field identifiers and permissions are configured.
- Full desktop test chain passed on 2026-07-15, including the new approval, approval-store, XiaoMiFeng export, and Feishu-contract tests. Project-memory verification and PGY safety audit passed. MVP readiness remains blocked only by the missing sanitized real-account validation record.

- Clean Git repository established at `xhs-pgy-data-collector`.
- Main product direction chosen: Electron desktop app in `desktop-app/`; legacy Python/FastAPI backend in `content-analyzer/` remains for compatibility.
- Batch task workflow exists with serial processing, manual pause/intervention, skip current task, and run/evidence output.
- Export/resource-table, local SQLite sync, KB search, and AI chat support exist in the desktop app.
- Reliability reporting added: runs can include `quality_report.json`; task queues can write `task_state.json`.
- Path opening from renderer is guarded to allowed run/export-related directories.
- Project-local workflow skill exists at `skills/pgy-desktop-workflow/`.
- Signing/search task context was introduced in `desktop-app/lib/signing_task.js` and the batch task UI, so runs can carry business criteria such as task name, search channels, contact plan, and filter criteria.
- Signing/search tasks can now be saved, reused, deleted, and listed with recent execution records through `desktop-app/lib/signing_task_store.js` and `userData/signing_tasks/`.
- Execution records now summarize `quality_report.json` files from each run/sub-run, including issue counts, lowest score, missing fields, failed pages, and an "open run directory" action in the task UI.
- External manuals and packages were reviewed at a product/architecture level: the Feishu Copilot flow is valuable as a complete signing chain, the Automa package is a packaged extension artifact and should not become a build target, and XiaoMiFeng is a packaged Windows RPA executable suitable only as a possible Excel-consuming downstream tool after template/result behavior is verified.
- Added `docs/project_memory/PRODUCT_ARCHITECTURE_ROADMAP.md` to preserve the product-chain direction, RPA limits, vendor evidence checklist, and staged refactor plan.
- First contact-list export and review pool were added in `desktop-app/lib/contact_sheet.js` and `desktop-app/renderer/views/exports.js`: the results export page can preview creators, mark selected/excluded, add priority/contact/notes/exclusion reason, then generate a workbook with a standard `建联表` sheet and an optional selected-only `小蜜蜂导入表` sheet from collected `raw_result.json` files.
- Contact review state now persists per run through `desktop-app/lib/contact_review_store.js` under Electron `userData/contact_reviews/`, so operator selections, notes, contact fields, exclusion reasons, and default contact copy can be restored later without writing runtime review data into the repository.
- The contact review pool now supports keyword search plus filters for selected/excluded, missing/filled contact info, priority/no-priority, and follow-up status. Batch select/unselect and batch follow-up-status changes can be applied to the current filtered result set. The current filtered result set can also be copied as Pugongying URLs, copied as a compact review summary, or exported as its own timestamped four-sheet contact workbook for teammate handoff; that export filename includes key active filters. It autosaves review edits, shows save status, and also provides a manual "保存复核" action for explicit run-level persistence.
- The contact review pool summary now shows both whole-run and current-filter counts for contact-ready rows and missing-contact rows, so operators can decide whether the filtered set should go to XiaoMiFeng export or teammate completion.
- The results export page remembers the latest contact workbook path from full or filtered contact export and provides an "打开最近建联表" action, reusing the existing runs/export path guard.
- The results export page can import teammate-edited contact review workbooks through `desktop-app/lib/contact_review_excel.js`; imported `建联表` rows are matched back to the current preview by normalized Pugongying URL, then saved into run-level contact review state. Import summaries include unmatched row examples to help detect wrong run/table mismatches.
- The task page now has a lightweight Pugongying discovery entry: open the Pugongying creator plaza, copy a search checklist built from the signing task criteria, and add the current Pugongying creator page from the embedded browser into the candidate queue.
- Signing/search tasks now have an explicit source mode: `已有达人表/链接`, `蒲公英搜索发现`, or `导入 + 搜索`. Existing Excel/URL import remains supported, while Pugongying search discovery is modeled as a first-class manual-login/platform-filter workflow that feeds the candidate queue before collection.
- The candidate queue on the task page can now be searched, filtered by status, copied, edited with per-creator labels/notes, priority, candidate status, and exclusion reason, then cleaned up before starting collection. Operators can remove one candidate, remove all currently filtered candidates, copy only currently displayed URLs, or batch mark the current filtered set as selected/candidate/excluded.
- The candidate queue can now export a pre-collection Excel workbook through `desktop-app/lib/candidate_sheet.js`; it includes a `候选初筛表` sheet plus a `筛选条件` sheet for teammate review before collection starts.
- Excel import now uses `desktop-app/lib/pgy_excel.js` as the single parsing module and can read status, priority, exclusion reason, and note fields from exported candidate screening workbooks. The task page has explicit replace-import and merge-import buttons, so teammate-edited `候选初筛表` files can be imported back without losing candidates that are not in the returned sheet.
- Saved signing/search tasks now include the current candidate queue and collection scope, so a task can restore its search criteria, candidate Pugongying links, creator labels, notes, priority, status, exclusion reasons, and whether to collect active/selected/all candidates instead of treating the candidate pool as a one-time textarea.
- The task page now warns when candidate queue or collection-scope changes are not saved into the signing/search task yet; the warning clears after "保存任务和候选" succeeds or after loading a saved task, and start-run confirmation also reminds operators that the current run will use unsaved page state.
- Candidate decisions are carried into run metadata and used as default contact-review values in `desktop-app/lib/contact_sheet.js`; saved per-run review edits still override candidate defaults.
- The task page now includes pre-run checks for candidate count, collection scope/count, note coverage, selected template, criteria count, and Pugongying login status. Starting collection shows a confirmation summary and warnings before launching the serial run. Default collection scope is "selected + candidate" and skips excluded creators while retaining them in task/run metadata.
- The task page and execution records now include a "go to review/export" path that switches to the results export page with the relevant run directory preselected.
- The Pugongying search-discovery flow now has a current-page intake path: after the operator manually filters/searches on the right-side `找博主` page, `读取当前结果` reads the current visible creator result links and a lightweight filter snapshot into the candidate pool. This is the preferred search-page flow before batch collection; `加入当前达人` remains for single detail pages.
- The results export page now auto-loads the contact review preview for the selected/preselected run, while still keeping the manual refresh action available.
- The results export page now distinguishes contact-preview empty states: no selected run, still loading, preview load error, no `raw_result.json`, raw files found but no usable creator rows, and current filters hiding all rows.
- Contact workbook export now separates downstream execution readiness: `小蜜蜂导入表` only contains selected creators with a WeChat ID or phone number, while selected creators missing contact info are placed in a separate `待补联系方式` sheet for manual completion.
- `待补联系方式` is now a round-trip completion sheet: it includes blank WeChat/phone columns, and `desktop-app/lib/contact_review_excel.js` imports both `建联表` and `待补联系方式`, merging completed contact info back into the run-level review state by normalized Pugongying URL.
- Contact review now tracks and manages `跟进状态` across local review state, preview UI, `建联表`, and Excel import. Default status is `待建联` for selected creators and `不建联` for excluded creators; the review page can filter by follow-up status and batch-update the current filtered result set. The optional XiaoMiFeng sheet remains limited to executable contact rows.
- Contact workbook export now includes a first `建联概览` sheet with total creators, selected/excluded counts, contact-ready rows, pending-contact rows, and follow-up-status counts. The export success message also shows the follow-up-status distribution.
- Contact workbook export now supports downstream execution channels inspired by the external signing Copilot screenshots, but keeps execution manual/export-first: selected creators can be routed to `蒲公英邀约表`, `邮件建联表`, `小蜜蜂导入表`, or `待补联系方式`. The review UI stores per-creator `建联方式` plus email/WeChat/phone fields and default invite/email copy. It does not auto-send emails, auto-click Pugongying invite buttons, or control WeChat.
- Added `docs/project_memory/UX_REDESIGN_PLAN.md` as the UI/product-workbench redesign blueprint. It captures the design rules being applied: progressive disclosure, importance/frequency ordering, non-permanent bulk actions, list/detail patterns, and lightweight components that can later map to React/Vue.
- The Electron workbench UI has started moving to the UX redesign blueprint: sidebar navigation is flow-first (`开始` / `找达人` / `复核建联`) with lower-frequency tools grouped below; shared native UI helpers live in `desktop-app/renderer/ui/components.js`.
- The `找达人`, `采集规则`, `录制回放`, `复核建联`, and `AI 分析` pages now use progressive disclosure for advanced settings or low-frequency operations. `复核建联` uses a card/list pattern with per-creator detail expansion for contact fields and notes instead of exposing every field as a dense table.
- The workbench shell now has a first-pass "达人建联工作台" information architecture: primary sidebar entries are `开始`, `找达人`, `复核建联`, `采集校准`, `AI 分析`, and `工具箱`. `录制回放` remains available from the toolbox instead of living in the main daily-flow navigation.
- The start page now presents three main action cards: open Pugongying, find creators, and review/export contact lists. The task page now surfaces a `任务简报` before detailed criteria, hides run-directory details from the default hero, and keeps detailed filters/history in an uncommon-use section. The calibration page now frames selector editing as "第一次教系统采集" and keeps technical fields behind the uncommon rule-file section.
- The calibration flow now borrows the Codex browser-annotation pattern: operators choose a business field on the left, enter annotation mode, click a numbered block directly in the embedded browser, and save that page position into the existing template structure. A left-side candidate list and manual hover/click selection remain as fallbacks, so selector details stay hidden in normal use.
- The annotation-style calibration was refined after checking Codex's official in-app browser annotation docs: Codex browser comments are a review/feedback feature, not an embeddable SDK. The app therefore keeps a local implementation. `进入网页标注` now uses mouse-follow highlighting so only the current hovered area is emphasized and clicked to save; the full-page numbered candidate overlay is kept behind `只看候选列表` as a backup because showing all blue boxes at once made live pages unreadable.
- Added `docs/project_memory/PGY_ANTI_BOT_SAFETY_AUDIT.md` after reviewing anti-bot/risk-control posture. The mainline should reduce risk through visible BrowserView operation, manual login/search, serial low-frequency collection, risk-page pauses, and small batches. Do not add stealth/browser-fingerprint/proxy evasion to the product path.
- `desktop-app/lib/task_runner.js` now uses more conservative jittered wait presets, keeps note-link click completion disabled, caps normal runs at 50 creators, and pauses when `pgy:checkLogin` reports risk-control text. The task page pre-run panel now shows the recommended 10-30 creator batch size, the 50 creator hard ceiling, and the risk-page pause behavior. Legacy `content-analyzer/` browser evasion settings are gated behind explicit `ALLOW_STEALTH_EVASION=true`, and legacy headless/proxy defaults are now off.
- The normal task UI now exposes only `标准（保守）` and `更保守（更慢）` collection presets, so the operator flow does not advertise a speed-seeking preset.
- `scripts/audit_pgy_safety.py` now checks repository-level anti-bot safety invariants and should be run after any collection-speed, batch-size, browser automation, proxy/headless, or risk-control change.
- `pgyExtractCurrentMultiPage` now rechecks risk-control text during extraction and before writing results. If a safety verification/frequent-operation page appears after opening the URL or after a tab click, extraction returns `PGY_RISK_DETECTED` and the task runner pauses for manual intervention instead of treating it as an ordinary field extraction failure.
- A repository-wide anti-bot scan found two extra risk surfaces and reduced them: the bundled `default_pgy_v1.json` no longer defaults to note-card click completion, `pgyExtractCurrentMultiPage` no longer falls back to click completion when templates omit the setting, and recording replay now stops on visible risk-control text before/after navigation, click, or input actions. The older `content-analyzer/docs/SECURITY_IMPROVEMENTS.md` file is now explicitly marked as deprecated historical notes, not current safety guidance.
- Hidden task-payload bypasses are closed in `desktop-app/lib/task_runner.js`: the 50-creator ceiling is unconditional, `allowLargeBatch` is no longer honored, `payload.options` cannot re-enable note-link click completion, and custom tab waits cannot shorten below the selected conservative preset.
- `desktop-app/lib/task_runner.js` now rejects immediate back-to-back batches with `PGY_RUN_COOLDOWN`: after a completed queue, the next valid batch must wait at least 5 minutes. The task UI safety notice also tells operators to keep this gap between batches.
- Batch cooldown is now persisted under the local runs directory as `.pgy_task_cooldown.json`, so restarting the Electron app does not clear the 5-minute safety gap. This file contains only timestamps and should not contain account or creator data.
- Batch task URLs are now allowlisted to `pgy.xiaohongshu.com` in `desktop-app/lib/task_runner.js`. Non-PGY URLs are rejected with `PGY_TASK_URL_NOT_ALLOWED`, so the desktop app does not behave like a generic browser automation runner if the operator imports the wrong link.
- Direct current-page extraction now also requires the right-side BrowserView to be on `pgy.xiaohongshu.com`. `pgy:extractCurrentMultiPage` returns `PGY_CURRENT_URL_NOT_ALLOWED` before creating run output if the current page is not PGY.
- Recording replay now also enforces the PGY domain boundary: non-PGY navigation/click/input replay stops with `PGY_REPLAY_URL_NOT_ALLOWED`, and replay still stops on visible risk-control text.
- The recording/replay page now shows an explicit safety notice: replay only allows Pugongying pages, and captcha/human-verification/access-abnormal/frequent-operation hints stop replay. This keeps the low-frequency maintenance page from looking like a general automation accelerator.
- Page-calibration annotation entrypoints are now PGY-only too: `pgy:pickElement`, `pgy:scanPageBlocks`, and `pgy:suggestNoteCardSelector` reject non-PGY current pages with `PGY_CURRENT_URL_NOT_ALLOWED`. The Codex-like annotation pattern remains a usability reference, not a general webpage automation tool.
- Recording capture is now PGY-only as well: `recording:start` rejects non-PGY current pages, and navigation/click/input actions are not stored if the BrowserView leaves `pgy.xiaohongshu.com`. This keeps the low-frequency recording tool scoped to Pugongying troubleshooting rather than generic browser automation.
- Added `docs/project_memory/PGY_LIVE_VALIDATION_PROTOCOL.md` and `scripts/validate_pgy_live_validation.py` for sanitized real-account validation records. This makes the remaining 3-5 creator and 10 creator live checks explicit and machine-checkable without committing private creator/account data.
- A follow-up safety scan reduced old compatibility-layer bypasses: `BaseCrawler(use_proxy=True)` and `BrowserEngine(headless=True)` can no longer bypass `ALLOW_STEALTH_EVASION`; `helpers.py` no longer imports `fake_useragent` at module load and returns a stable UA unless the explicit research gate is enabled; nonstandard Selenium flags/preferences such as disabling web security, site isolation, certificate checks, or images are gated behind `ALLOW_STEALTH_EVASION`.
- The legacy FastAPI crawler/prelogin routes are now disabled by default. `/api/crawl/start` and `/api/prelogin/*` require `ENABLE_LEGACY_CRAWL_API=true`; if explicitly enabled, old crawl tasks are still capped by `LEGACY_CRAWL_MAX_URLS` and `LEGACY_CRAWL_MAX_CONTENTS`. The Electron mainline does not rely on these routes.
- The backend now defaults to `API_HOST=127.0.0.1`, and its root page shows a disabled legacy-collection notice when `ENABLE_LEGACY_CRAWL_API=false` instead of loading the old crawler UI.
- Added `scripts/probe_pgy_runtime_safety.py` to verify a running backend reports proxy/headless off, legacy crawl API disabled, `/api/crawl/start` returning 403, and the disabled legacy root page.
- Closed two remaining direct-call safety gaps: task payloads requesting legacy `presetKey: "fast"` now normalize to `standard`, and direct `pgy:extractCurrentMultiPage` calls keep a 2500 ms tab-wait floor plus disabled note-card click completion unless the explicit local research switch `PGY_ALLOW_NOTE_CLICK_RESOLVE=true` is set. `scripts/audit_pgy_safety.py` now checks these invariants.
- PGY risk text detection now covers broader visible prompts beyond basic captcha/safety wording: human verification, identity verification, drag-slider prompts, access-abnormal pages, account/login-expired prompts, busy/retry pages, English verification/captcha/security-check text, and `too many requests`.
- PGY visible-risk text detection now lives in `desktop-app/lib/pgy_risk.js` with unit coverage in `desktop-app/tests/pgy_risk.test.js`. `main.js` uses this shared module to generate the BrowserView risk-detection snippet, and `scripts/audit_pgy_safety.py` checks that this testable boundary stays in place.
- Replaced old evasion-oriented compatibility docs. `content-analyzer/README.md` now describes the directory as a legacy compatibility backend with safe defaults, and `content-analyzer/docs/SECURITY_IMPROVEMENTS.md` is now a deprecation notice instead of a source of fingerprint/proxy/headless/cookie guidance. `scripts/audit_pgy_safety.py` now checks that these docs do not regress to the old guidance.
- Added `scripts/prepare_pgy_live_validation.py` and upgraded live-validation records to schema v2. The preparer creates a sanitized JSON record with current branch/worktree note, Electron/backend PIDs, `scripts/audit_pgy_safety.py` result, and `scripts/probe_pgy_runtime_safety.py` result prefilled before any real-account testing. `scripts/validate_pgy_live_validation.py` now requires those prechecks for a passing record.
- Tightened schema-v2 live-validation records further: completed records must replace placeholder aliases, choose `office/home/other`, include local start/end times, include local-only evidence refs for both batches, choose a concrete risk-stop method, and avoid URL/secret shapes. `scripts/test_pgy_live_validation.py` covers these validator cases.
- Added `docs/project_memory/MVP_ACCEPTANCE_PLAN.md` and `scripts/check_mvp_readiness.py` to make the delivery boundary explicit. The project should not be described as delivered until local readiness, safety gates, and sanitized real-account validation are all satisfied.
- On 2026-07-20, added a separate Xiaohongshu public-contact enrichment step to the contact-review workbench. It resolves a creator profile from the PGY creator page, visits profiles serially in the visible BrowserView, requires manual Xiaohongshu login, pauses on login/risk prompts, extracts only public email/WeChat/phone values, never overwrites manual contact fields, and does not change the selected contact channel automatically. Full profile text is not persisted.
- On 2026-07-28, the rank-range reader was corrected so an incomplete page sequence is no longer reported as success or merged into the candidate pool. When the target crosses rank 40 or 80, the app now keeps the partial rows only in memory, locks the collection page, pauses for a 90-second conservative cooldown, and requires an explicit user continue. Continue rechecks the fixed collection tab, exact URL, page number, current-page creator anchors, and PGY risk text. The 90-second value is an engineering default, not an official safe value and not a guarantee against platform risk.
- The embedded browser now has a visible tab strip with a protected `采集` tab and a separate closable `企业邮箱` tab. PGY collection, candidate pagination, calibration, recording, replay, and the first-stage XHS flow remain bound to the protected collection BrowserView; Tencent mail uses a separate persistent Electron session with no recording preload. Browser navigation, tab switching, and page pointer input are locked during active automation, while task manual-intervention pauses unlock page input but keep the collection tab selected.
- Candidate page turns now wait up to 15 seconds for a new response, verify that the visible page number advanced, and reject duplicate pages. Missing next controls, response timeouts, page mismatches, duplicate pages, risk prompts, and incomplete ranges return distinct failures; partial rows are not automatically committed.

## Checked Environment State

Latest source and installed-artifact checks on 2026-08-05:

- Repository path and branch were rechecked as `C:\Users\feibo\Documents\Codex\2026-06-25\s\projects\xhs-pgy-data-collector` on `main`; the working tree remains intentionally dirty with prior and current local work. The 30-file desktop test chain and PGY safety audit passed after the global-rank/current-page binding correction.
- Local NSIS upgrade to `0.1.5` succeeded. The installed executable reports product version `0.1.5.0`; the visible app reported `服务正常 / v0.1.5`; `/api/desktop/health` returned protocol 1, a launch token, and the packaged backend PID. Installed ASAR inspection confirmed visible-name response binding, current-page DOM/component fallback, and React/Vue discovery.
- Before upgrade, the logged-in PGY page visibly contained about 20 creators on page 1. After upgrade the protected BrowserView reopened at `about:blank`; login state, current search filters, and live DOM remain volatile. No real candidate import, page turn, enrichment, export, invitation, or send action was performed with `0.1.5`, so a logged-in first-21 or first-50 test is still required before calling cross-page production behavior verified.

Previous checks on 2026-08-03:

- Repository path and branch were rechecked as `C:\Users\feibo\Documents\Codex\2026-06-25\s\projects\xhs-pgy-data-collector` on `main`. The working tree is intentionally dirty with the ongoing 0.1.2 optimization and prior local work; remote sync and exact diff scope remain volatile.
- The 29-file desktop test chain passed. `scripts/audit_pgy_safety.py`, `scripts/test_mvp_readiness.py`, `scripts/test_pgy_live_validation.py`, and `scripts/verify_project_memory.py` passed. `scripts/check_mvp_readiness.py` reported `pass=32 fail=0 block=1 warn=0`; the sole block is the missing sanitized real-account validation record.
- The self-contained portable build and upgraded installed build both started without system Python or Node on their runtime path. `/api/desktop/health` reported protocol version 1, a nonempty launch token, and the packaged `xhs-pgy-backend` PID. The second launch retained one window and the same backend PID; app close stopped the backend and released port 8010.
- The installed UI was visibly checked at the normal window size. Sidebar/topbar/content rendered, `采集页` and `企业邮箱` were visible, enterprise mail opened in its separate BrowserView, switching back restored the protected collection view, and closing the mail tab restored the `+ 企业邮箱` action. No PGY candidate read, XHS enrichment, mailbox login, export, invitation, or send was triggered.

Previous checks on 2026-07-31:

- On 2026-07-31, the repository path and `main` branch were rechecked. The actual installed ASAR was dated July 27 and lacked both the new pagination error strings and `browserTabRegistry`; both user shortcuts had nonexistent `C:\Users\CodexSandboxOffline\...` targets. These installed-state facts are volatile until `0.1.1` is built and installed.
- Current `0.1.1` installer: `desktop-app/dist/小红书相关信息采集工具-0.1.1-windows-x64-setup.exe`, 108,717,115 bytes, SHA-256 `6AE515973EE1F65C8E5B41D25927748A863611079693D04EDFFCE96B91A24046`. The `win-unpacked` executable reports product version `0.1.1.0`; the packaged ASAR contains the response-evidence pagination path and visible tab labels. Recheck file hash after any rebuild.
- Repository path was checked as `C:\Users\feibo\Documents\Codex\2026-06-25\s\projects\xhs-pgy-data-collector`; branch was `main`. The working tree contains the ongoing Windows packaging and 2026-07-28 browser/candidate changes, so cleanliness and remote sync must be rechecked.
- All 28 desktop test files passed after adding candidate-checkpoint, browser-tab-registry, process-stdio, and task-page-identity coverage. `scripts/audit_pgy_safety.py` passed. Live PGY rank 35-50 continuation, real mailbox login, and NSIS install/upgrade/uninstall acceptance remain unverified and require explicit visible testing.
- `scripts/check_mvp_readiness.py` reported `pass=32 fail=0 block=1 warn=0`; the remaining blocker is still the missing sanitized live-validation record, so the app must not yet be described as fully delivered.
- The rebuilt Windows `win-unpacked` app was visibly checked on this machine: no main-process error dialog appeared, the embedded backend reached `服务正常`, the collection tab retained `about:blank`, and `+ 邮箱` opened the official Tencent enterprise-mail site in a separate closable tab. Closing the app released port 8010. This is directory-build evidence, not clean-machine or installed-NSIS acceptance.
- Current installer: `desktop-app/dist/小红书相关信息采集工具-0.1.0-windows-x64-setup.exe`, 108,722,717 bytes, SHA-256 `02F5767F2D2B885417102634713CA816B932D173664A23AC90B5AEAD51BDD46C`. It is unsigned and still uses Electron's default icon; file presence and hash must be rechecked after any rebuild.

Latest checks on 2026-07-22:

- A live XHS contact-enrichment attempt reached the Xiaohongshu security-verification page with an English request-frequency warning after nine unsuccessful items. This is evidence of temporary session/IP rate limiting, not evidence of an account ban. The old XHS detector missed the English wording and captcha URL, so those pages were recorded as profile load failures instead of pausing immediately. The fix adds testable Chinese/English XHS risk patterns, captcha-URL detection, preflight rejection while risk-blocked, a disabled retry state in the UI, and a 35-60 second cooldown after each five profiles. The updated installed app reached `服务正常`; the XHS page was not reopened or refreshed after installation. Source checkpoint: `05b86d1` / `snapshot-2026-07-22-xhs-rate-limit-guard`; recovery time, session status, network state, and remote sync remain volatile.
- The final local recovery archive is `releases/2026-07-22/小红书相关信息采集工具-2026-07-22-xhs风控修复最终版.app.zip` (about 145 MB), SHA-256 `c5f62dd88112f0a784dc40cf2bdf2939ecfaa900463f5d6ae8e9007d462e0dba`. ZIP integrity passed. `releases/` is local-only and Git-ignored; archive presence and remote sync remain volatile and must be rechecked.
- Contact review now exposes always-visible `全选` and `重置` actions for the entire collected run. Reset clears selection only and preserves collected/contact fields. Same-view rerenders retain the outer workbench scroll, and the creator list restores its own scroll after being attached to the DOM; the bulk action section also retains its open state. Installed-app validation passed at 30/30 select-all, 0/30 reset, and consecutive selections at rows 6 and 7 while the list remained around rows 5-7. The original selected rows 1, 2, 6, and 7 were restored afterward. No enrichment, export, or outreach was started. Source checkpoint: `339d756` / `snapshot-2026-07-22-stable-review-selection`; remote sync status must be rechecked.
- The public-contact enrichment action is now permanently visible in the Xiaohongshu status toolbar as `补采已选达人联系方式`. It is disabled at zero selected creators with an explicit selection hint, becomes enabled only after manual `要建联` selection, and no longer offers a conflicting first-N action inside a collapsed advanced section. The installed app was checked with the live Xiaohongshu session: zero selected, one temporary selection, and zero selected again; no enrichment or outreach was started. All 23 Node tests and the PGY safety audit passed. Source checkpoint: `beecc7e` / `snapshot-2026-07-22-visible-enrichment-action`; remote sync status must be rechecked.
- Repository branch is `main`; the mail/link iteration is preserved at `c68dc3e` / `snapshot-2026-07-22-email-handoff`, and the workbench layout iteration is preserved at `6efd2c9` / `snapshot-2026-07-22-workbench-layout`. Remote sync status remains volatile and must be rechecked.
- The official Tencent enterprise-mail landing page resolved to `https://work.weixin.qq.com/mail/` in an isolated Electron profile. Login state, post-login DOM, recipient tokens, and account-specific verification remain volatile and require a user-authorized live check.
- No real creator page, mailbox account, invitation, or send action was used in UI testing. Dummy addresses and dummy Pugongying URLs were stored only under a temporary test user-data directory.
- The installed desktop app now defaults to about 72% workbench and 28% embedded browser, with a 320px browser minimum and a draggable splitter. The installed start and contact-review pages were visually checked at the normal application window size; 23 Node tests passed and the local service reached `服务正常`. Process ownership and running state remain volatile and must be rechecked after restart.
- Contact review now uses manual opt-in: new and legacy review rows default to unchecked, and the email action handles only explicitly selected creators. Missing public emails are enriched through the existing serial XHS flow; valid emails are shown for manual copying before the app opens Tencent enterprise mail. Compose clicking, recipient prefilling, and the renderer IPC bridge for prefilling were removed. The installed 20-row run was checked at 0 selected, then one existing-email row was tested and returned to 0 selected without composing or sending. All 23 Node tests and the PGY safety audit passed. Source checkpoint: `b31a0f3` / `snapshot-2026-07-22-manual-email-handoff`; remote sync status must be rechecked.

Latest checks on 2026-07-20:

- Repository path: `/Users/jingjing/Documents/Codex/2026-06-03/new-chat/product/xhs-pgy-data-collector`; branch `main`. The validated 2026-07-20 iteration is saved as a local Git checkpoint; commit hash, remote status, and worktree cleanliness must be rechecked at the start of the next thread.
- The installed backend answered at `127.0.0.1:8010`; the observed “service not running” UI was stale startup state, not a failed backend. Recheck after restart or packaging.
- `pnpm test` passed all 19 test files. `python3 scripts/verify_project_memory.py` and `python3 scripts/audit_pgy_safety.py` passed. `python3 scripts/check_mvp_readiness.py` reported `pass=32 fail=0 block=1`; the block is the missing sanitized live validation record.
- The installed `.app` has not been replaced by this source build. Visual acceptance, a XiaoMiFeng test import, and real Feishu writes remain unverified.
- Desktop dependencies were restored and all 20 Node test files passed, including `xhs_contact_enrichment.test.js`. Safety audit passed; readiness remains `pass=32 fail=0 block=1`, with sanitized real-account validation still missing.
- The source Electron review page was visually checked at 1195x768 using an isolated temporary profile; the enrichment controls fit without obvious overlap. No creator profile was visited and no outreach was sent.
- The source Python backend failed to start because `fastapi` is absent. A separate installed app was already serving the same health port, so the green UI status is not evidence that the source backend passed. Recheck process ownership before packaging or backend acceptance.
- A one-creator live enrichment passed on 2026-07-20 after manual Xiaohongshu login: PGY profile resolution, visible Xiaohongshu navigation, public-email parsing, review-store persistence, and UI status all completed (`processed=1`, `found=1`, `failed=0`). No risk prompt appeared and no outreach was sent. The first attempt exposed a Chromium `-3/ERR_ABORTED` signal after the correct profile had already loaded; this is now tolerated only when the canonical current and target profile URLs match, with unit coverage.
- The enrichment action now applies only to selected creators inside the current review filter. This allowed a one-person trial without changing the other 49 selection states or starting the full batch.

Older checked state from 2026-07-02 (historical and volatile):

- Repository path: `/Users/workstudio/Downloads/数据收集/xhs-pgy-data-collector`
- Git branch: `main`
- Remote: `origin` points to `https://github.com/toastphoto/xhs-pgy-data-collector.git`
- `git status --short`: working tree contains many local modified/untracked files from the ongoing refactor and feature work; do not assume a clean tree.
- Port `8010`: backend is listening at `127.0.0.1:8010` with Python PID `32974`; the Electron dev chain is `node ./scripts/run-electron.js` PID `32957` and `Electron .` PID `32958` during the latest check. This is volatile and should be rechecked before visual QA or live workflow testing.
- Runtime safety probe: `python scripts/probe_pgy_runtime_safety.py` passed against the current backend. This verifies `/api/config` reports proxy/headless off and legacy crawl API disabled, `/api/crawl/start` returns HTTP 403, and `/` shows the disabled legacy-collection notice. Recheck after restart or environment changes.
- Latest checks passed after the MVP readiness gate change: `python3 scripts/check_mvp_readiness.py`, `python3 scripts/test_mvp_readiness.py`, `python3 scripts/check_mvp_readiness.py --run-commands`, `python3 scripts/verify_project_memory.py`, `python3 -m py_compile scripts/check_mvp_readiness.py scripts/test_mvp_readiness.py scripts/verify_project_memory.py`, and `git diff --check`. The readiness report currently shows `pass=36 fail=0 block=1`, with the only blocker being the missing sanitized real-account live validation record. Re-run relevant checks after further edits; this line is volatile.
- Local task/input files `蒲公英链接.txt` and `任务队列_*.xlsx` are ignored by `.gitignore`.

All items in this section are volatile and must be rechecked before making new changes.

## Must Recheck

- Git branch, working tree, and remote sync state.
- Whether the unsigned Windows installer passes SmartScreen handling, uninstall, clean-company-machine startup, Chinese user/install paths, BrowserView login, and workbook export on the target company device. Local upgrade/startup/backend-cleanup passed on 2026-08-03 but does not prove those remaining environments.
- Whether a Windows code-signing certificate and final application icon are available before external distribution.
- Whether the current unsigned NSIS installer completes install/uninstall, start-menu shortcut, and SmartScreen handling on the target company machine. Local upgrade and desktop-shortcut checks passed for `0.1.2` only.
- Whether `desktop-app/node_modules/` exists and is healthy.
- Whether the Electron app or Python backend is currently running.
- If the old FastAPI crawl/prelogin API is ever intentionally enabled with `ENABLE_LEGACY_CRAWL_API=true`, recheck the risk boundary and do not treat that mode as the normal product path.
- Whether local ignored task/link files are present before import/export work.
- Whether GitHub CLI/auth/proxy status is needed for push, PR, or CI work.
- Whether Pugongying/Xiaohongshu page DOM selectors still match current live pages.
- Whether later PGY DOM or response changes regress the installed `0.1.9` cross-page acceptance for global first-50 and direct rank 35-50 commands; re-run both visible flows after relevant reader, pagination, or platform changes.
- Whether protected-tab switching and the automation navigation lock remain correct during an authorized live PGY collection; installed idle-state tab open/switch/close behavior passed on 2026-08-03.
- Whether a real Pugongying account can run 3-5 and then 10 creator checks without triggering risk-control pages, recorded via `scripts/validate_pgy_live_validation.py`; do not infer this from unit tests or static safety audits.
- Whether the XiaoMiFeng RPA package, Feishu manuals, and WeChat RPA website have changed since the last inspection.
- Whether XiaoMiFeng supports a stable import template, result export, command-line trigger, API trigger, or stable local database schema.
- Whether Tencent enterprise mail still uses the same official host and post-login compose DOM; revalidate with a dummy or approved mailbox before relying on automatic recipient filling.

## Suggested Next Steps

1. Keep splitting high-risk logic out of `desktop-app/main.js` into testable modules.
2. Add fixture-based extraction tests using sanitized saved HTML, without committing real account data.
3. Improve data quality reporting in the UI with per-creator drilldown, not only run-level summaries.
4. Run a sanitized recovery drill for a deliberately interrupted synthetic batch before relying on unfinished-run recovery for long production queues.
5. Continue refining the final exported workbook shape, especially once a real XiaoMiFeng import template, Pugongying invite template, email-sending policy, or downstream WeChat AI format is available.
6. Refine the XiaoMiFeng-compatible Excel sheet after a real sanitized import template or result export sample is available; keep rows missing contact info in round-trippable `待补联系方式` until they are actionable.
7. Consider Lark Base/Sheets sync only after local export, quality reporting, and task state are stable.
8. Continue the UI redesign from `UX_REDESIGN_PLAN.md` in order: whole-app structure, Find Creators workflow, Contact Review workflow, then Toolbox pages. Avoid isolated cosmetic changes that do not reduce cognitive load.
9. Extend annotation-style calibration after real-page validation: tune candidate scoring for live Pugongying pages, add Shift area selection if needed, and capture lightweight screenshot evidence for saved blocks.
10. Run the anti-bot safety validation in `docs/project_memory/PGY_ANTI_BOT_SAFETY_AUDIT.md` before increasing speed, batch size, or adding any deeper automation.
11. Use `scripts/check_mvp_readiness.py --run-commands` as the first readiness report before deciding whether to polish UI, fix static gaps, or start real-account validation.
12. After the operator completes manual Xiaohongshu login, run the public-contact enrichment on 3-5 sanitized creators first. Validate profile resolution, parser accuracy, and pause-on-risk behavior before expanding to the original 50-person run.
